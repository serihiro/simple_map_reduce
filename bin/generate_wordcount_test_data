#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'faker'

base_path = File.realpath('../../examples', __FILE__)
file = File.open(File.join(base_path, 'wordcount', 'input.txt'), 'w')
file.write(Faker::Lorem.paragraphs(1_000_000).join("\n"))
file.close
